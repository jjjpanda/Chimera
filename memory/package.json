{
  "name": "memory",
  "main": "server.js",
  "scripts": {
    "prestart": "cd .. && npm-run-all -s --npm-path npm prepare:env",
    "start": "node start.js",
    "test": "jest --verbose",
    "prunewrap": "npm shrinkwrap"
  },
  "dependencies": {
    "socket.io": "^4.4.0",
    "socket.io-client": "^4.4.0",
    "lib": "file:../lib",
    "node-cron": "^3.0.0",
    "dotenv": "^10.0.0"
  }
}
